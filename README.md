# dexim-cli-common

Shared Rich UI/UX library for DexImitate CLI applications.

Provides a common `Theme`, `Console` singleton, app banner, table builders, and error handler so every `dexim-*` CLI looks and behaves identically.
